#include <vector>
#include <string>
int no_arg();
int num_arg(int a);
double num_argd(double a);
int numnum_arg(int a, int b);
int *arr(int arr[]);
std::vector<int> vect(std::vector<int> ve);
std::string stri(std::string st);
int loop(int x);